import CollectionHeader from '../components/admin/CollectionHeader'

const Blogs = ({ collectionName }) => {
	return (
		<>
			<CollectionHeader collectionName={collectionName} />
		</>
	)
}

export default Blogs
